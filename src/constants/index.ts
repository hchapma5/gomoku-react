export enum TILE_STATUS {
  EMPTY = 'EMPTY',
  BLACK = 'BLACK',
  WHITE = 'WHITE',
}

export enum PLAYER {
  BLACK = 'BLACK',
  WHITE = 'WHITE',
}

export enum GOMOKU_BOARD_SIZE {
  DEFAULT = 15,
  MIN = 5,
  MAX = 19,
}
