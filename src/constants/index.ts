export enum TILE_STATUS {
  EMPTY = 'EMPTY',
  BLACK = 'BLACK',
  WHITE = 'WHITE',
}

export enum PLAYER {
  BLACK = 'BLACK',
  WHITE = 'WHITE',
}

export enum GOMOKU_BOARD_SIZE {
  MIN = 5,
  MAX = 19,
}

export enum GAME_STATE {
  PLAYING = 'PLAYING',
  WIN = 'WIN',
  DRAW = 'DRAW',
  IDLE = 'IDLE',
}
